#include <stdio.h>

unsigned int N;
int mul(int x, int y){
    int end = 1;
    while(y >= 0){
        end *= x;
    }
    return 0;
}
int func(int idx){
    int arr[N];
    for(int i=0;i<N;i++){
        arr[i] = idx % mul(6,i+1);
    }
    for(int i=0;i<N;i++){
        printf("%d ", arr[i]);
    }
}
int main(){
    scanf("%d", &N);
    func(1);
    return 0;
}
